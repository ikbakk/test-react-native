import React from "react"
import Icon from 'react-native-vector-icons/Octicons'

export default function TabBarIconProfil({ size, color }) {
  return (
    <Icon
      name='person'
      size={size}
      color={color}
    />
  )
}