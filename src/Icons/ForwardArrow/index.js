import React from "react"
import Icon from 'react-native-vector-icons/MaterialIcons'

export default function ForwardArrow({ size, color }) {
  return (
    <Icon
      name='arrow-forward-ios'
      size={size}
      color={color}
    />
  )
}