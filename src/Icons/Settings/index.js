import React from "react"
import Icon from 'react-native-vector-icons/Feather'

export default function SettingIcon({ size, color }) {
  return (
    <Icon
      name='settings'
      size={size}
      color={color}
    />
  )
}