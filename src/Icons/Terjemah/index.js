import React from "react"
import Icon from 'react-native-vector-icons/MaterialCommunityIcons'

export default function TabBarIconTerjemah({ size, color }) {
  return (
    <Icon
      name='google-translate'
      size={size}
      color={color}
    />
  )
}