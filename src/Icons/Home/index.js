import React from "react"
import Icon from 'react-native-vector-icons/Feather'

export default function TabBarIconHome({ size, color }) {
  return (
    <Icon
      name='home'
      size={size}
      color={color}
    />
  )
}